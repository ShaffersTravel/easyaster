# Example environment variables for development
# Copy this file to `.env.local` and replace the placeholder values.


# Stripe API keys
#
# For local development you may use Stripe test keys (beginning with `pk_test_` and `sk_test_`).
# Before launching the site, replace these with your **live** keys (beginning with `pk_live_` and `sk_live_`) to
# process real payments.  Keys should never be committed to version control.
STRIPE_PUBLIC_KEY=pk_test_your_public_or_live_key
STRIPE_SECRET_KEY=sk_test_your_secret_or_live_key

# Stripe price IDs for each plan (test mode).  You can create Products and Prices in your
# Stripe dashboard and copy their price IDs here.  These values are read in
# `app/api/checkout/route.ts`.
PRICE_BASIC=price_your_basic_id
PRICE_PREMIUM=price_your_premium_id
PRICE_CONCIERGE=price_your_concierge_id

# Base URL of your site (e.g. http://localhost:3000 or https://visa.example.com)
NEXT_PUBLIC_SITE_URL=http://localhost:3000

# Admin password for accessing the admin page.  Choose a strong password for
# production.
ADMIN_PASSWORD=change_me

# Umami analytics configuration
#
# Set these variables to integrate a privacy‑friendly analytics tool such as Umami.
# `NEXT_PUBLIC_UMAMI_SCRIPT_URL` should be the full URL of the Umami script
# (e.g. https://analytics.example.com/script.js).
# `NEXT_PUBLIC_UMAMI_WEBSITE_ID` is the UUID provided by your Umami instance for this site.
NEXT_PUBLIC_UMAMI_SCRIPT_URL=
NEXT_PUBLIC_UMAMI_WEBSITE_ID=

# UptimeRobot API key
#
# When deploying the site, you can use this API key with the provided setup script
# to create uptime monitors for the home page and other endpoints.  Leave blank
# during development and supply a real key in production.
UPTIMEROBOT_API_KEY=

# hCaptcha configuration
#
# To protect your intake form from bots, you can enable hCaptcha.  Set
# `NEXT_PUBLIC_HCAPTCHA_SITE_KEY` to the site key provided by hCaptcha for
# your domain.  Set `HCAPTCHA_SECRET_KEY` to the secret key used to verify
# captcha responses on the server side.  Leave these blank during
# development if you do not wish to enable captcha verification.
NEXT_PUBLIC_HCAPTCHA_SITE_KEY=
HCAPTCHA_SECRET_KEY=